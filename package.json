{
  "name": "boilerplate",
  "version": "1.0.0",
  "description": "This is a boilerplate",
  "scripts": {
    "start": "rm -rf dev-serve && parcel src/*.html --dist-dir dev-serve ", 
    "build": "rm -rf docs && parcel build src/*.html --dist-dir docs --public-url ./", 
    "publish": "gh-pages -d build"
  },
  "author": "mf",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "bootstrap": "^5.2.0"
  }
}
